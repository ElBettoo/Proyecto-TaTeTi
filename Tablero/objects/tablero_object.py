from abc import ABC, abstractmethod

class TableroObject(ABC):
    def __init__(self):
        self.__name = name

    #@abstractmethod
    #def get_default_casillero(self): # Devuelve el casillero default o vacio dependiendo del tipo de juego que se quiera poner
        #pass    